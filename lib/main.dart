import 'package:flutter/material.dart';

// void main() {
//   runApp(DemoApp());
// }

void main() => runApp(DemoApp());

class DemoApp extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('Hell o world'),
    );
  }
}
